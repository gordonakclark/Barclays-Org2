# Copilot Instructions

## Project Guidelines
- User prefers role names shortened/abbreviated in the org chart and UI, e.g. 'Chief Information Officer' should be abbreviated to 'CIO'.
- Always use Visual Studio instead of VS Code for this user/workspace.